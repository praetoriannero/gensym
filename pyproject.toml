[project]
name = "gensym"
version = "0.1.0"
dependencies = [
    "black>=25.1.0",
    "matplotlib>=3.10.3",
    "networkx>=3.5",
    "numpy>=2.3.1",
    "pytest>=8.4.1",
    "tqdm>=4.67.1",
]

[build-system]
requires = ["setuptools>=80.0.0"]
build-backend = "setuptools.build_meta"
